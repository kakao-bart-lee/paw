# paw-android

Phase 1 placeholder for the future native Android app shell.

For now this directory only reserves the top-level app root and the `app/src/main/jniLibs` output path used by `scripts/build-core-android.sh`.
