[project]
name = "paw-agent-sdk"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["websockets>=12.0", "pydantic>=2.0"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "pytest-asyncio>=0.23"]
