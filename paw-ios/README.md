# paw-ios

Phase 1 placeholder for the future native iOS app shell.

For now this directory only reserves the top-level app root and the `PawCore/Artifacts` output path used by `scripts/build-core-ios.sh`.
